name: dfm_plus
description: "DFM+ danmaku engine — Flutter rendering layer for the Rust-based DFM+ layout engine"
version: 0.1.0
homepage: https://github.com/nipaplay/DFM+

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
