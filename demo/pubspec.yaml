name: dfm_plus_demo
description: DFM+ native GPU demo player
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  dfm_plus:
    path: ../flutter
  dfm_plus_native:
    path: ../native_plugin

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
